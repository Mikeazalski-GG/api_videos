services:
  - type: web
    name: api-agente-videos
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api_video:app --host 0.0.0.0 --port $PORT
    plan: free # Garante que usará o plano gratuito